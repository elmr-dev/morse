# Absolute site origin for canonical / Open Graph / sitemap URLs. Read at build
# time by vite.config.ts (loadEnv) and by scripts/prerender.ts. Deploys are built
# locally and uploaded, so this lives in the repo, not a hosting dashboard.
# Cloudflare cutover later = change this one line.
VITE_SITE_URL=https://bejewelled-manatee-c93aa4.netlify.app
